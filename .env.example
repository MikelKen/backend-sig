# Variables de entorno para la base de datos PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASS=postgres
DATABASE_NAME=back_sig

# Puerto de la aplicación
PORT=3000

# Ambiente
NODE_ENV=development

# JWT Secret (opcional, para autenticación) 
JWT_SECRET=tu_jwt_secret_aqui

# Para Docker Compose, crea también un .env.docker
# DATABASE_HOST=postgres_db (nombre del servicio en docker-compose)